--- a/drivers/vservices/session.c
+++ b/drivers/vservices/session.c
@@ -1904,10 +1904,7 @@
 		struct vs_mbuf *mbuf, vs_service_id_t service_id)
 {
 	struct vs_service_device *service;
-	struct vs_transport *transport;
 	unsigned long flags;
-
-	transport = session->transport;
 
 	service = vs_session_get_service(session, service_id);
 	if (!service) {
--- a/arch/arm64/kernel/vmlinux.lds.S
+++ b/arch/arm64/kernel/vmlinux.lds.S
@@ -5,6 +5,7 @@
  * Written by Martin Mares <mj@atrey.karlin.mff.cuni.cz>
  */
 
+#define BSS_FIRST_SECTIONS RTIC_BSS
 #include <asm-generic/vmlinux.lds.h>
 #include <asm/cache.h>
 #include <asm/kernel-pgtable.h>
@@ -23,6 +24,9 @@
 ENTRY(_text)
 
 jiffies = jiffies_64;
+
+/* Stock SM8250 exports this symbol with CRC 0 (unversioned). */
+PROVIDE(__crc_gsi_write_channel_scratch = 0);
 
 
 #define HYPERVISOR_EXTABLE					\
@@ -261,9 +265,6 @@
 
 	HEAD_SYMBOLS
 
-	.bss : {			/* bss segment		*/
-         RTIC_BSS
-	}
 }
 
 /*
--- a/fs/namespace.c
+++ b/fs/namespace.c
@@ -32,9 +32,7 @@
 
 #ifdef CONFIG_OPLUS_SECURE_GUARD
-#ifdef CONFIG_OPLUS_MOUNT_BLOCK
 #ifdef CONFIG_OPLUS_KEVENT_UPLOAD
 #include <linux/oplus_kevent.h>
 #endif /* CONFIG_OPLUS_KEVENT_UPLOAD */
-#endif /* CONFIG_OPLUS_MOUNT_BLOCK */
 #endif /* CONFIG_OPLUS_SECURE_GUARD*/
 
