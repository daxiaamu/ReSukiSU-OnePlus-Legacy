--- a/kernel/msm-4.19/techpack/display/oplus/oplus_display_panel.c
+++ b/kernel/msm-4.19/techpack/display/oplus/oplus_display_panel.c
@@ -478,7 +478,7 @@
 	return rc;
 }
 
-void __exit oplus_display_panel_exit()
+void __exit oplus_display_panel_exit(void)
 {
 	pr_info("%s\n", __func__);
 
--- a/vendor/oplus/kernel/touchpanel/oplus_touchscreen/touchpanel_common_driver.c
+++ b/vendor/oplus/kernel/touchpanel/oplus_touchscreen/touchpanel_common_driver.c
@@ -1670,7 +1670,7 @@
 }
 EXPORT_SYMBOL(tp_gpio_current_leakage_handler);
 
-bool tp_boot_mode_normal()
+bool tp_boot_mode_normal(void)
 {
     if (!g_tp) {
         return false;
--- a/vendor/oplus/kernel/system/uboot_log/uboot_log.c
+++ b/vendor/oplus/kernel/system/uboot_log/uboot_log.c
@@ -167,7 +167,7 @@
 /*
  * Dont printk any log int this thread
  */
-int ubootback_thread_fn()
+int ubootback_thread_fn(void *unused)
 {
 	size_t line_len = 0;
 	u32 idx =0;
--- a/vendor/oplus/kernel/network/data_module/dpi/dpi_core.c
+++ b/vendor/oplus/kernel/network/data_module/dpi/dpi_core.c
@@ -1082,7 +1082,7 @@
 		for (k = 0; k < stream_count; k++) {
 			speedRsp.streamuidspeed[k] = (Netlink__Proto__DpiSpeedItem *)
 						((char *)speedRsp.streamuidspeed + sizeof(void *) * stream_count + sizeof(Netlink__Proto__DpiSpeedItem) * k);
-			netlink__proto__dpi_speed_item__init((ProtobufCMessage *)speedRsp.streamuidspeed[k]);
+			netlink__proto__dpi_speed_item__init(speedRsp.streamuidspeed[k]);
 		}
 		uid_count = 0;
 		hlist_for_each_entry(pos_app, &s_match_app_result_head, node) {
@@ -1238,7 +1238,7 @@
 		for (k = 0; k < stream_count; k++) {
 			speedRsp.streamuidspeed[k] = (Netlink__Proto__DpiSpeedItem *)
 						((char *)speedRsp.streamuidspeed + sizeof(void *) * stream_count + sizeof(Netlink__Proto__DpiSpeedItem) * k);
-			netlink__proto__dpi_speed_item__init((ProtobufCMessage *)speedRsp.streamuidspeed[k]);
+			netlink__proto__dpi_speed_item__init(speedRsp.streamuidspeed[k]);
 		}
 		hlist_for_each_entry(pos_all_uid, &s_match_uid_result_head, node) {
 			if (ifidx_count == 0) {
--- a/vendor/oplus/kernel/network/data_module/tmgp_sgame/wzry_stats.c
+++ b/vendor/oplus/kernel/network/data_module/tmgp_sgame/wzry_stats.c
@@ -575,7 +575,7 @@
 			Netlink__Proto__GameDelayStats *stats = (Netlink__Proto__GameDelayStats *)((char *)getGameDelay.stats
 				+ copy_count * sizeof(Netlink__Proto__GameDelayStats *) + i * sizeof(Netlink__Proto__GameDelayStats));
 			getGameDelay.stats[i] = stats;
-			netlink__proto__game_delay_stats__init((ProtobufCMessage *)stats);
+			netlink__proto__game_delay_stats__init(stats);
 			stats->timestamp = copy_delay_arry[i].timestamp;
 			stats->rx_ip_delay = copy_delay_arry[i].rx_ip_delay;
 			stats->rx_udp_delay = copy_delay_arry[i].rx_udp_delay;

diff --git a/vendor/oplus/kernel/touchpanel/oplus_touchscreen/touchpanel_common_driver.c b/vendor/oplus/kernel/touchpanel/oplus_touchscreen/touchpanel_common_driver.c
--- a/vendor/oplus/kernel/touchpanel/oplus_touchscreen/touchpanel_common_driver.c
+++ b/vendor/oplus/kernel/touchpanel/oplus_touchscreen/touchpanel_common_driver.c
@@ -1596,3 +1596,20 @@
+int tp_control_cs_gpio(bool enable)
+{
+	if (!g_tp) {
+		return 0;
+	}
+
+	if (gpio_is_valid(g_tp->hw_res.cs_gpio)) {
+		if (g_tp->ts_ops->cs_gpio_control) {
+			g_tp->ts_ops->cs_gpio_control(g_tp->chip_data, enable);
+		}
+	}
+
+	return 0;
+}
+
+EXPORT_SYMBOL(tp_control_cs_gpio);
+
 int check_touchirq_triggered(void)
 {
     int value = -1;
diff --git a/vendor/oplus/kernel/touchpanel/oplus_touchscreen/touchpanel_common.h b/vendor/oplus/kernel/touchpanel/oplus_touchscreen/touchpanel_common.h
--- a/vendor/oplus/kernel/touchpanel/oplus_touchscreen/touchpanel_common.h
+++ b/vendor/oplus/kernel/touchpanel/oplus_touchscreen/touchpanel_common.h
@@ -1032,3 +1032,4 @@
+	int (*cs_gpio_control)(void *chip_data, bool enable);
 	u8 (*trigger_reason)(void *chip_data, int gesture_enable, int is_suspended);               /*clear innterrupt reg && detect irq trigger reason*/
 	u32 (*u32_trigger_reason)(void *chip_data, int gesture_enable, int is_suspended);
 	u8 (*get_keycode)(void *chip_data);                                                        /*get touch-key code*/
--- a/vendor/oplus/kernel/touchpanel/oplus_touchscreen/Synaptics/Syna_tcm_oncell/synaptics_tcm_oncell.h
+++ b/vendor/oplus/kernel/touchpanel/oplus_touchscreen/Synaptics/Syna_tcm_oncell/synaptics_tcm_oncell.h
@@ -118,6 +118,7 @@
 #define VEE_DETECT      0x05
 #define HEART_DETECT    0x07
 #define UNICODE_DETECT  0x08
+#define STAP_DETECT     0x10
 #define M_UNICODE       0x6d
 #define W_UNICODE       0x77
 
--- a/vendor/oplus/kernel/touchpanel/oplus_touchscreen/Synaptics/Syna_tcm_oncell/synaptics_tcm_oncell.c
+++ b/vendor/oplus/kernel/touchpanel/oplus_touchscreen/Synaptics/Syna_tcm_oncell/synaptics_tcm_oncell.c
@@ -3349,6 +3349,9 @@
         }
         break;
 
+    case STAP_DETECT:
+        gesture->gesture_type = SingleTap;
+        break;
     case TRIANGLE_DETECT:
     default:
         TPD_DEBUG("not support\n");
@@ -3367,6 +3370,11 @@
         gesture->Point_3rd.y    = (touch_data->data_point[18] | (touch_data->data_point[19] << 8));
         gesture->Point_4th.x    = (touch_data->data_point[20] | (touch_data->data_point[21] << 8));
         gesture->Point_4th.y    = (touch_data->data_point[22] | (touch_data->data_point[23] << 8));
+    }
+
+    if (gesture->gesture_type == SingleTap || gesture->gesture_type == DouTap) {
+        gesture->Point_start.x = (touch_data->extra_gesture_info[0] | (touch_data->extra_gesture_info[1] << 8));
+        gesture->Point_start.y = (touch_data->extra_gesture_info[2] | (touch_data->extra_gesture_info[3] << 8));
     }
 
     TPD_INFO("lpwg:0x%x, type:%d, clockwise: %d, points: (%d, %d)(%d, %d)(%d, %d)(%d, %d)(%d, %d)(%d, %d)\n",
